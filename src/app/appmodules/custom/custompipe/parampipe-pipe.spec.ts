import { ParampipePipe } from './parampipe-pipe';

describe('ParampipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParampipePipe();
    expect(pipe).toBeTruthy();
  });
});
