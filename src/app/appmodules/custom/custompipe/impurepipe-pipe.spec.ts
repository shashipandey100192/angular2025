import { ImpurepipePipe } from './impurepipe-pipe';

describe('ImpurepipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurepipePipe();
    expect(pipe).toBeTruthy();
  });
});
